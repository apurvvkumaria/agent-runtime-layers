# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local secrets (LangFuse keys, etc.)
.env

# Persisted conversation memory
.agent_history.json

# Default output of `agent research`
report.md

# Test / eval caches
.pytest_cache/
.deepeval/
.deepeval*.json

# Vector store memory (persisted embeddings)
vector_store/
chroma_db/

# Autonomous-mode runtime state
tasks.json
# DLQ runtime state (keep the package code, ignore the JSON stores)
dlq/*.json

# Eval response cache (Layer 22) — runtime state
evals/.cache/

# Local Claude Code state
.claude/

# Marketplace-installed skills (Layer 27) — source of truth is marketplace/
skills/error_budget/
