"""MCP integration: consume an external MCP server (client) and expose the agent
as an MCP server (server).

Note: this package is named `mcp_integration`, not `mcp`, because a top-level
`mcp/` directory would shadow the installed `mcp` SDK and break `from mcp import ...`.
"""
